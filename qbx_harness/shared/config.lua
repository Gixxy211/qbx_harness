Config = {}

-- If you use the np fake plate system, set this to true
Config.npFakePlate = false

-- Time in milliseconds to install the harness (Progress Bar)
Config.InstallTime = 10000

-- Harness item name (ox_inventory item name)
Config.Harness = 'harness'

-- If enabled, using a harness in a vehicle that already has one
-- will uninstall the existing harness
Config.UninstallHarnessWithItem = true
